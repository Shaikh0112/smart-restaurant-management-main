// RESPONSIBILITY: Server-side entry point for Manager Onboarding page.
// DATA FLOW: Server -> ManagerOnboardingClient

import ManagerOnboardingClient from './ManagerOnboardingClient';

export default function OnboardingPage() {
  return <ManagerOnboardingClient />;
}
