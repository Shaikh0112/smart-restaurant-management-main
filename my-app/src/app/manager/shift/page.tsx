// RESPONSIBILITY: Server-side entry point for Manager Shift page.
// DATA FLOW: Server -> ManagerShiftClient

import ManagerShiftClient from './ManagerShiftClient';

export default function ShiftPage() {
  return <ManagerShiftClient />;
}
